import { LandingPage } from "@/templates/landing-page";


export default function HomePage() {
  return (
    <LandingPage />
  );
}

/**
 * Roteamento no NEXT pode ser feito de duas formas:
 * 
 * Usando Page Router
 * / -> pages/index.tsx 
 *
 * Usando App Router 
 * / -> app/pages.tsx
 */
