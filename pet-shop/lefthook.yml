pre-commit:
  commands:
    check:
      root: "pet-shop"
      glob: "**/*.{js,jsx,ts,tsx,css,json,md}"
      run: npm run format -- {staged_files}
      stage_fixed: true

pre-push:
  commands:
    validate:
      root: "pet-shop"
      glob: "**/*.{ts,tsx}"
      run: npm run validate:typecheck