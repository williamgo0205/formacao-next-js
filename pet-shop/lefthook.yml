pre-commit:
  commands:
    check:
      run: npm format {staged_files}
      glob: '*.{js,jsx,ts,tsx,css,json,md}'
      stage_fixed: true

pre-push:
  commands:
    validate:
      glob: '*.{ts,tsx}'
      run: npm validate:typecheck